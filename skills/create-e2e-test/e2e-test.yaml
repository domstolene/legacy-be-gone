name: E2E - Playwright

on:
  workflow_run:
    workflows: ["CD - Build docker images on pushes to main"]
    types: [completed]
    branches: [main]
  schedule:
    - cron: "0 6 * * *" # kjør hver morgen kl 8 sommertid, 7 vintertid
  workflow_dispatch:

jobs:
  e2e:
    # hopp over dersom CD workflowen feiler, for å unngå unødvendige testkjøringer
    if: ${{ github.event_name != 'workflow_run' || github.event.workflow_run.conclusion == 'success' }}
    runs-on: workflow-runner-test
    timeout-minutes: 15
    permissions:
      contents: read
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24

      - name: Enable Corepack
        run: corepack enable

      - name: Activate pnpm
        run: corepack prepare pnpm@latest --activate

      - name: Install dependencies
        working-directory: src/test/e2e
        run: pnpm install

      - name: Sett forventet git SHA
        id: sha
        run: |
          if [ "${{ github.event_name }}" = "workflow_run" ]; then
            echo "value=${{ github.event.workflow_run.head_sha }}" >> $GITHUB_OUTPUT
          else
            echo "value=${{ github.sha }}" >> $GITHUB_OUTPUT
          fi

      - name: Vent på at riktig versjon kjører i testmiljøet
        timeout-minutes: 14
        env:
          BASE_URL: ${{ vars.BASE_URL || 'https://<applikasjon>.apps.ocp.test.domstol.no' }} # eller http://<applikasjon>.<namespace>.svc.cluster.local (obs: service krever AuthorizationPolicy)
          EXPECTED_SHA: ${{ steps.sha.outputs.value }}
        run: |
          SHORT_SHA="${EXPECTED_SHA:0:7}"
          echo "Venter på sha=$SHORT_SHA i $BASE_URL/version"
          until curl -sf "$BASE_URL/version" | grep -q "\"sha\":\"$SHORT_SHA\""; do
            RUNNING=$(curl -sf "$BASE_URL/version" | grep -o '"sha":"[^"]*"' || echo 'utilgjengelig')
            echo "Kjører nå: $RUNNING – venter 30 sekunder..."
            sleep 30
          done
          echo "Riktig versjon ($SHORT_SHA) kjører"

      - name: Run Playwright tests
        working-directory: src/test/e2e
        env:
          BASE_URL: ${{ vars.BASE_URL || 'https://<applikasjon>.apps.ocp.test.domstol.no' }} # eller http://<applikasjon>.<namespace>.svc.cluster.local (obs: service krever AuthorizationPolicy)
          PROSJEKT: ${{ github.event.repository.name }}
          EXPECTED_SHA: ${{ steps.sha.outputs.value }}
        run: pnpm test:ci

      - name: Upload Playwright HTML report as Pages artifact
        if: always()
        uses: actions/upload-pages-artifact@v5
        with:
          path: src/test/e2e/playwright-report

      - name: Deploy to GitHub Pages
        if: always()
        id: deployment
        uses: actions/deploy-pages@v4
